import React, { Component} from "react";
import {hot} from "react-hot-loader";
import "css/App";

function App() {
    return(
      <div className="App">
        <h1> Hello, World!</h1>
      </div>
    );
}

export default hot(App);